# pi_voice — xiaozhi-server custom LLM provider that routes voice turns
# through the dotty-pi container (replaces zeroclaw / tier1_slim per #36).
# Skeleton: implementations land in subsequent sessions.
